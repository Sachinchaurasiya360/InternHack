const express = require("express");
const app = express();
const router = express.Router();
const user=

router.use("/user", user);
router.use("/superadmin", surperadmin);
router.use("/recuriters", recuriters);
router.use("/organiser", organiser);

module.exports = router;
